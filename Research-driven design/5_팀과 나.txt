후속 구조화된 파싱 및 다운 애플리케이션:
원시 데이터(텍스트, 로그, 이미지 메타데이터 등)를 
기계가 이해할 수 있도록 구조화된 형태로 변환하는 과정 (팀)

+

이렇게 구조화된 데이터를 후속 단계의 응용 프로그램에서 활용하는 것. 
즉, 파싱된 결과를 기반으로 검색, 추천, 질의응답, 분석 같은 
실제 서비스에 적용하는 단계  (나)

->
1. json파일 받아서 db 만들고, 매칭 로직을 짜며, 설명 생성 (챗봇?)

2. *환각 필드 -> 검증 로직 필요

3. 제조 db (로컬 공구 데이터베이스)

팀이 도면에서 json 뽑아오면 내가 json파일 -> db 검색 -> 설명 생성

더미 json 만들고, 더미 db 만든 후에, 매칭코드 짜기


